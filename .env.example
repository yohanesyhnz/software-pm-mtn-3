# ASP.NET Core Web API origin.
BACKEND_ORIGIN=http://127.0.0.1:5080

# Backend-only PostgreSQL settings. Fill these in an untracked environment
# file or secret manager; never prefix them with NEXT_PUBLIC_.
POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_DATABASE=
POSTGRES_USER=
POSTGRES_PASSWORD=
